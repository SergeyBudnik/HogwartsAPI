package com.bdev.hogwarts_api.data.dto.student;

public enum StudentStatusType {
    REQUEST,
    LEFT_BEFORE_TEST,

    TEST_ASSIGNED,
    STOPPED_AFTER_TEST,
    LEFT_AFTER_TEST,

    FREE_LESSON_ASSIGNED,
    STOPPED_AFTER_FREE_LESSON,
    LEFT_AFTER_FREE_LESSON,

    AWAITING_GROUP,
    STOPPED_WHILE_AWAITING_GROUP,
    LEFT_WHILE_AWAITING_GROUP,

    STUDYING,
    STOPPED_STUDYING,
    LEFT_STUDYING
}
